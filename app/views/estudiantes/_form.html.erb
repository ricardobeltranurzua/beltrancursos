<%= form_with(model: estudiante) do |form| %>
  <% if estudiante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estudiante.errors.count, "error") %> prohibited this estudiante from being saved:</h2>

      <ul>
        <% estudiante.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :apellido %>
    <%= form.text_field :apellido %>
  </div>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.select :curso_id, @cursos.map {|curso| [curso.nombre, curso.id]}, prompt: "Seleccione" %>
  </div>
  

  <div class="actions">
    <%= form.submit "Guardar" %>
  </div>
<% end %>
